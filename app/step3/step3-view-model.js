const observableModule = require("data/observable");

function Step3ViewModel() {
    const viewModel = observableModule.fromObject({
        /* Add your view model properties here */
    });

    return viewModel;
}

module.exports = Step3ViewModel;
